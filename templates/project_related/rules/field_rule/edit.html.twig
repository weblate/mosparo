{% extends 'project_related/rules/field_rule/form.html.twig' %}

{% trans_default_domain 'mosparo' %}

{% block documentTitle %}{{ 'rules.fieldRule.edit.title'|trans({ '%ruleName%': rule.name }) }} - {{ 'rules.fieldRule.title'|trans }} - {{ parent() }}{% endblock %}
{% block pageTitle %}{{ 'rules.fieldRule.edit.title'|trans({ '%ruleName%': rule.name }) }}{% endblock %}

{% block breadcrumbsContainer %}
    {% set breadcrumbs = breadcrumbs|merge([
        {'title': 'rules.fieldRule.title'|trans, 'url': path('rules_field_rule_list', {'_projectId': activeProject.id})},
    ]) %}

    {{ parent() }}
{% endblock %}
