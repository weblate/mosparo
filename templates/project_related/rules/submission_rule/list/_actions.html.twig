{% trans_default_domain 'mosparo' %}

<div class="d-flex action-buttons-list">
    <a href="{{ path('rules_submission_rule_configure', { '_projectId': activeProject.id, 'key': item.key}) }}" class="btn btn-primary">
        <i class="ti ti-settings"></i>
        {{ 'action.configure'|trans }}
    </a>
</div>
