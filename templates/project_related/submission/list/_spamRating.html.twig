{% if item.spamRating >= item.spamDetectionRating %}
    <span class="text-danger">
{% else %}
    <span class="text-success">
{% endif %}
    <strong>{{ item.spamRating }} / {{ item.spamDetectionRating }}</strong>
</span>
