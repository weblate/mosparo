{% extends 'project_related/settings/settings_base.html.twig' %}

{% trans_default_domain 'mosparo' %}

{% block documentTitle %}{{ 'settings.translation.title'|trans }} - {{ parent() }}{% endblock %}
{% block pageTitle %}{{ 'settings.translation.title'|trans }}{% endblock %}

{% block pageTitleColumn %}
    {{ parent() }}

    <div class="col-auto ms-auto">
        <div class="btn-list">
            <a href="https://documentation.mosparo.io/docs/usage/settings#translations" class="btn btn-info" target="_blank">
                <i class="ti ti-question-mark"></i>
                {{ 'action.help'|trans }}
            </a>
            <a href="{{ path('settings_translation_add', { '_projectId': activeProject.id }) }}" class="btn btn-success add-button">
                <i class="ti ti-plus"></i>
                <span>{{ 'settings.translation.add.title'|trans }}</span>
            </a>
        </div>
    </div>
{% endblock %}

{% block settingsPageBody %}
    <div class="row">
        <div class="col">
            {% include 'ui/grid_table/table.html.twig' with {'table': table, 'identifier': 'translations'} %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {% cspscript %}
        <script>
            $(function () {
                new GridTable(
                    $('#translations'),
                    [
                        ['(width >= 1200px)', 'min-content 1fr 1fr min-content'],
                        ['(width >= 450px) and (width < 1200px)', 'min-content 1fr 1fr'],
                        ['(width < 450px)', 'min-content 1fr'],
                    ]
                );
            });
        </script>
    {% endcspscript %}
{% endblock %}
