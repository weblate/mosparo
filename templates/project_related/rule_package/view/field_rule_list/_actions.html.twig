{% trans_default_domain 'mosparo' %}

<div class="d-flex action-buttons-list">
    <a href="{{ path('rule_package_view_field_rule', { '_projectId': activeProject.id, 'id': item.rulePackageCache.rulePackage.id, 'ruleUuid': item.uuid }) }}" class="btn btn-info">
        <i class="ti ti-info-circle"></i>
        {{ 'action.view'|trans }}
    </a>
</div>
