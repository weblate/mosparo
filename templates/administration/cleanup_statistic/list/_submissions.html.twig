{% trans_default_domain 'mosparo' %}

{{ value }}

{% if item.numberOfDeletedSubmissions %}
    <span class="status status-red" data-bs-toggle="tooltip" data-bs-placement="bottom" title="{{ 'tooltip.deleted'|trans }}">
        -{{ item.numberOfDeletedSubmissions }}
    </span>
{% endif %}
